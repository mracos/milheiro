{
  "extends": "./.wxt/tsconfig.json",
  "compilerOptions": {
    // WXT defaults this on; our code predates it. Keeping it off keeps the
    // migration logic-free (revisit as a separate change if we want it).
    "noUncheckedIndexedAccess": false
  }
}
